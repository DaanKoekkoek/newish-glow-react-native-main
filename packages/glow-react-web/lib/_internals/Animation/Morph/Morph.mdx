import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as MorphStories from "./Morph.stories";

<Meta of={MorphStories} />

# Morph

<Canvas of={MorphStories.Default} />
<Controls of={MorphStories.Default} />
