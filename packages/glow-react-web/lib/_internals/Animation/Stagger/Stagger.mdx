import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as StaggerStories from "./Stagger.stories";

<Meta of={StaggerStories} />

# Stagger

<Canvas of={StaggerStories.Default} />
<Controls of={StaggerStories.Default} />
