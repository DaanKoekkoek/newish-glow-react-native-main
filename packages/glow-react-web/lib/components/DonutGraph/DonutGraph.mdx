import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as DonutGraphStories from "./DonutGraph.stories";

import { getComponentVersion } from "@odido-portals/glow-tokens/component-versions";

<Meta of={DonutGraphStories} />

# DonutGraph

#### Token version: {getComponentVersion("donut-graph.v1")}

The `DonutGraph` component allow to show percentage of remaining time, it can be configured with label, value and percentage.

<Canvas of={DonutGraphStories.Default} />
<Controls of={DonutGraphStories.Default} />
