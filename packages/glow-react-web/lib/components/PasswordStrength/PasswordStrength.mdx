import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as PasswordStrengthStories from "./PasswordStrength.stories";
import { getComponentVersion } from "@odido-portals/glow-tokens/component-versions";

<Meta of={PasswordStrengthStories} />

# PasswordStrength

### Token version: {getComponentVersion("skip")}

A strength indicator of the password that is provided to the component as a property.

<Canvas of={PasswordStrengthStories.Basic} />
<Controls of={PasswordStrengthStories.Basic} />

## Basic PasswordStrength

A basic PasswordStrength.

<Canvas of={PasswordStrengthStories.Basic} />

## Localized PasswordStrength

A localized PasswordStrength. All the content inside the component can be localized.

<Canvas of={PasswordStrengthStories.Localized} />
