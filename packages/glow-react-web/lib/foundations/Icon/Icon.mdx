import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as IconStories from "./Icon.stories";

<Meta of={IconStories} />

# Icon component

Icon component is composed with icon fonts which are provided by another package `@odido-portals/glow-icon`

<Canvas of={IconStories.Basic} />
<Controls of={IconStories.Basic} />

## Icon collection

<Canvas of={IconStories.AllIcons} />
