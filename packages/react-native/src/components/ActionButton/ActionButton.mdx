import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as ActionButtonStories from "./ActionButton.stories";

<Meta of={ActionButtonStories} />

# Action Button

An action button is an button that contains an [`Action Button Icon`](?path=/docs/designsystem-components-actionbutton-icon--docs) and a label. The action button contains a `stretched link`, meaning that it makes its containing block clickable. Requires a container with `position: relative`.

<Canvas of={ActionButtonStories.Basic} />
<Controls of={ActionButtonStories.Basic} />

## Emphasised

<Canvas of={ActionButtonStories.Emphasised} />
