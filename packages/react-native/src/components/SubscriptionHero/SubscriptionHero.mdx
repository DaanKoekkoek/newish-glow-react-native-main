import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as SubscriptionHeroStories from "./SubscriptionHero.stories";

<Meta of={SubscriptionHeroStories} />

# Hero subscription

A subscription hero can include an illustration (or image) in addition to a title, a list items, and action buttons.
Please note that this component should connect edge-to-edge on `mobile` and `mobileSmall`, so make sure to not wrap it in a container with horizontal padding and margin.

<Canvas of={SubscriptionHeroStories.Basic} />
<Controls of={SubscriptionHeroStories.Basic} />

## Short list

An example of a SubscriptionHero with a shorter list.

<Canvas of={SubscriptionHeroStories.ShortList} />
