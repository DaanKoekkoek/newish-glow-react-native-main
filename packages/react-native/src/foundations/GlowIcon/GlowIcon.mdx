import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as GlowIcon from "./GlowIcon.stories";

<Meta of={GlowIcon} />

# GlowIcon

Uses the [GlowGradient component](?path=/docs/designsystem-utility-glowgradient--docs) in combination with an `Icon` to render a glow icon.
Currently only supported by the Odido brand.

## Basic GlowIcon

<Canvas of={GlowIcon.Basic} />
<Controls of={GlowIcon.Basic} />
