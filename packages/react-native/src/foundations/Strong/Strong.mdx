import { Canvas, Meta, Controls } from "@storybook/blocks";
import * as StrongStories from "./Strong.stories";

<Meta of={StrongStories} />

# Strong

The `Strong` component is used within the [`Paragraph`](?path=/docs/designsystem-typography-paragraph--docs) component.

<Canvas of={StrongStories.Basic} />
<Controls of={StrongStories.Basic} />
