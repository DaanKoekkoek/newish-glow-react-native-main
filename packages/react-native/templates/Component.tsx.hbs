import { View } from "react-native";
import { useStyles } from "react-native-unistyles";
import { {{pascalCase name}}Props } from './{{pascalCase name}}.types';
import { {{pascalCase name}}Styles } from './{{pascalCase name}}.styles';

export const {{pascalCase name}} = ({}: {{pascalCase name}}Props) => {
  const { styles } = useStyles({{pascalCase name}}Styles);
  return <View>{{pascalCase name}} Component</View>;
};
